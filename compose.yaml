services:
  bot:
    build:
      context: .
      target: dev
    env_file: .env
    volumes:
      - ./src:/app/src
      - ./tests:/app/tests
      - ./knowledge:/app/knowledge:ro
    restart: unless-stopped
